# Histórico

`aptis-general-b2.jsx` es una versión anterior y abandonada de la app: usa
otro modelo y no tiene los modos worker/banco de `index.html`, que es el
código que de verdad se ejecuta. Se conserva solo como referencia.
